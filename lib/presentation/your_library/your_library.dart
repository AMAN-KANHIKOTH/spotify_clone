import 'package:flutter/material.dart';

class ScreenYourLibrary extends StatelessWidget {
  const ScreenYourLibrary({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text(
      'Your Library',
      style: TextStyle(color: Colors.white),
    ));
  }
}
